创建两台虚拟机
		：服务端
		：客户端
scp /path root@ip:/path
4(r) 2(w) 1(x)
1  2 3 4 5 6 7
chmod 777 /root
软件和硬连接 | 链接
ln -s /etc/redhat-release
i节点 ：作用识别用盘的存储区
ls -i //查看i节点号
软链接：/ 对文件的快捷方式；针对文件某路径下的某文件或目录
功能：可跨分区磁盘；

硬链接：/对i节点做的快捷方式；指向磁盘快
不能跨分区磁盘；
wc -l 统计文件行数（）
另存为 ：w /mnt/a.txt
:r 读入其他文件内容
